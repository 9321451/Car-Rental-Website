import { AnimatedTextDirective } from "./animat-text.directive";

describe('AnimatTextDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimatedTextDirective();
    expect(directive).toBeTruthy();
  });
});
